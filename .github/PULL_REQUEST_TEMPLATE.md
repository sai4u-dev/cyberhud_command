## What changed

## Why (link issue / ADR)

## Contract impact

- [ ] No API change
- [ ] Backwards-compatible (new fields/endpoints only)
- [ ] Breaking (needs `/api/v2` + sunset note): …

## Verification

- [ ] `npm test` (backend) passes
- [ ] `npm run build` (frontend) passes
- [ ] Hit `/api/health` + `/api/docs` locally
- [ ] Screenshots (frontend change):

## Rollout / rollback

- Migrations? Feature flag? Rollback command?
